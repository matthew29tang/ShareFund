const users = require("./users");

const getEvents = (state) => {
    const login = (req, res) => {
    
    }
    
    const vote = (req, res) => {
    
    }
    
    const getstate = (req, res) => {
    
    }

    const events = {
        login: login,
        vote: vote,
        getstate: getstate
    }
}

module.exports = getEvents;